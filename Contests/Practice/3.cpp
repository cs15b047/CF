#include <bits/stdc++.h>
using namespace std ;
typedef long long int ll ;

int main(){
	ll t; scanf("%lld",&t);
	for(int q = 0;q <t;q++){
		ll n; scanf("%lld",&n);
		vector<string> str(n);for(int i = 0;i < n;i++){char s[5];		}
	}
}