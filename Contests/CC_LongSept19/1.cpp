#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;

int main(){
	ll t; cin >> t;
	for(int i = 0;i < t;i++){
		ll n, m; cin >> n >> m;
		if(m < n - 1 || m > n*n){
			cout << - 1 << endl;
		}else{
			
		}
	}
}